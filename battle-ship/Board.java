/*
 * class Board {
    -grid: Cell[][]
    -ships: List<Ship>
    +Board(rows: int, columns: int)
    +placeShip(ship: Ship): boolean
    +receiveShot(coord: Coordinate): boolean
    +allShipsSunk(): boolean
    +display(revealShips: boolean): void
}
 */
/*Board: matriz de Cell, gestión de disparos y colocación de barcos. */
public class Board {
    private Cell[][] grid;
    private
}
